// Pinned exact versions tested by both the unit-test CI matrix and the
// acceptance-test fixtures. Bump deliberately, not automatically.
module.exports = {
  showcase: '4.0.0', // version used on the public demo site
  tested: ['3.7.1', '4.0.0'], // versions the Playwright fixtures are generated for
};
